import 'package:flutter/material.dart';

import 'app.dart';

void main() {
  // Entry point of the application
  runApp(
    // Initialize the App widget, which is the root of the application
    const App(),
  );
}
